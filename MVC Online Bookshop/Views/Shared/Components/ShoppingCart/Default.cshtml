@inject IHttpContextAccessor HttpContextAccessor
@model Int32

<ul class="navbar-nav">
    <li class="nav-item">
        <a class="nav-link active" asp-area="Customer" asp-controller="Cart" asp-action="Index">
            <i class="bi bi-cart-fill fs-5"></i><span class="badge text-bg-danger ms-1" @(Model == 0 ? "hidden" : "")>@Model</span>
        </a>

    </li>
</ul>